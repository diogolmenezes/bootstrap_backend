const expect = require('chai').expect;
const sinon = require('sinon');

describe('Fatura Sevice', () => {

    const service = require('../../service/fatura-service');

    it('Deve ...', (done) => {
        done();
    })
});